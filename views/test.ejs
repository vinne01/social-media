<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ApnaSocialMedia</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <!-- Center the form on the page -->
    <div class="flex justify-center items-center min-h-screen">
  
      <!-- Form Container -->
      <div class="bg-white p-8 rounded-lg shadow-lg w-96">
        <h2 class="text-2xl font-semibold text-center text-gray-700 mb-6">Upload Files/ Documents</h2>
  
        <!-- Form with POST method and action -->
         <!-- multer is work if enctype="mutlipart/form-data added in form " -->
        <form action="/upload" method="POST" enctype="multipart/form-data">
  
          <!-- File Upload Field -->
          <div class="mb-4">
            <label for="file" class="block text-gray-700">Upload File</label>
            <input type="file" id="file" name="image" class="w-full p-3 mt-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
          </div>
  
          <!-- Submit Button -->
          <div class="mb-4">
            <button type="submit"
                    class="w-full py-3 bg-blue-500 text-white font-semibold rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">
              Upload File
            </button>
          </div>
  
        </form>
      </div>
    </div>
  
</body>
</html>
